ระบบวันลา (Leave Management System)" ให้ครบถ้วนสมบูรณ์แบบระดับ Enterprise และเข้ากับสถาปัตยกรรมเดิมของโปรเจกต์ (Next.js, Tailwind CSS และ Supabase PostgreSQL) สามารถแบ่งโครงสร้างออกเป็นส่วน ๆ ตามหลักการพัฒนาซอฟต์แวร์ได้ดังนี้ครับ:   1. โครงสร้างฐานข้อมูลเพิ่มเติม (Database Schema & Relations)เพิ่มตารางรองรับระบบลาให้สอดคล้องกับมาตรฐาน Relational Database (3NF):   ตารางประเภทวันลา (leave_types)id (UUID, Primary Key)   name (TEXT - เช่น ลาพักร้อน, ลาป่วย, ลากิจ, ลาไม่ได้รับค่าจ้าง)default_days (INTEGER - จำนวนวันเริ่มต้นตามสิทธิ์ต่อปี)is_paid (BOOLEAN - ได้รับค่าจ้างหรือไม่)ตารางคำขอลาของพนักงาน (leave_requests)id (UUID, Primary Key)   employee_id (UUID, Foreign Key $\rightarrow$ employees.id)   leave_type_id (UUID, Foreign Key $\rightarrow$ leave_types.id)   start_date (DATE, NOT NULL)   end_date (DATE, NOT NULL)   total_days (NUMERIC - จำนวนวันที่ลาจริง ไม่นับวันหยุด)reason (TEXT - เหตุผลการลา)attachment_url (TEXT - ลิงก์เก็บไฟล์ใบรับรองแพทย์ใน Supabase Storage)status (ENUM: PENDING, APPROVED, REJECTED, CANCELLED)approver_id (UUID, Foreign Key $\rightarrow$ employees.id - ผู้อนุมัติ)   created_at (TIMESTAMP)ตารางโควตาวันลาคงเหลือ (leave_balances)id (UUID, Primary Key)   employee_id (UUID, Foreign Key $\rightarrow$ employees.id)   leave_type_id (UUID, Foreign Key $\rightarrow$ leave_types.id)   year (INTEGER - ปี พ.ศ./ค.ศ.)total_quota (NUMERIC - สิทธิ์ทั้งหมดในปีนี้)used_days (NUMERIC - ใช้ไปแล้ว)remaining_days (NUMERIC - คงเหลือ)2. มุมมองการใช้งาน (System Portals)แบ่งฟังก์ชันการใช้งานออกเป็น 2 ส่วนหลักตามโครงสร้างเดิมของระบบ:Employee Self-Service (ESS) Portal:   Dashboard วันลาของฉัน: แสดงกราฟหรือการ์ดสรุปโควตาวันลาคงเหลือ (ลาพักร้อน, ลาป่วย, ลากิจ) แบบเรียลไทม์ฟอร์มยื่นขอลา: เลือกประเภทวันลา ระบุช่วงวันที่ ระบบคำนวณจำนวนวันให้อัตโนมัติ ช่องกรอกเหตุผล และอัปโหลดไฟล์แนบ (เช่น ใบรับรองแพทย์)ประวัติการลา: ตรวจสอบสถานะคำขอ (PENDING, APPROVED, REJECTED) และยกเลิกคำขอก่อนได้รับการอนุมัติHR Management Portal & Approver:   หน้าจออนุมัติคำขอ (Leave Approval Dashboard): สำหรับ HR หรือหัวหน้างานตรวจสอบรายชื่อพนักงานที่ยื่นลา พร้อมกดอนุมัติหรือปฏิเสธพร้อมระบุเหตุผลปฏิทินวันลาทีม (Team Leave Calendar): ปฏิทินแสดงภาพรวมว่าในแต่ละวันมีพนักงานคนไหนลาบ้าง เพื่อให้หัวหน้างานบริหารจัดการกำลังคนได้รายงานสรุปวันลา: ส่งออกรายงานเพื่อใช้ตรวจสอบอัตราการลาของพนักงานในแต่ละแผนก (departments)   3. ตรรกะทางธุรกิจและการเชื่อมโยงกับระบบเงินเดือน (Business Logic & Payroll Integration)การหักเงินเดือนอัตโนมัติ (Unpaid Leave Deduction):เมื่อพนักงานลาแบบไม่ได้รับค่าจ้าง (is_paid: false) หรือลาเกินโควตาสิทธิ์ที่กำหนด ระบบคำนวณเงินเดือน (Calculation Engine) จะนำจำนวนวันลามาหักออกจากฐานเงินเดือน (base_salary) ในรอบบัญชีนั้น ๆ ตามสูตรการคำนวณรายวันขององค์กร   การล็อกข้อมูลเพื่อความปลอดภัย (Data Integrity & Immutability):เมื่อรอบบัญชีเงินเดือนถูกประมวลผลและปิดงวดแล้ว ประวัติการลาที่เกี่ยวข้องในช่วงเวลานั้นจะถูกล็อกสถานะ (is_locked: true) เพื่อป้องกันการแก้ไขย้อนหลัง รักษาความถูกต้องของข้อมูลการจ่ายเงิน (Payroll Record)   4. หลักการออกแบบเชิงวิศวกรรมซอฟต์แวร์ (Software Engineering Principles)Single Responsibility Principle (SRP): แยกโค้ดส่วนการจัดการวันลา (Leave Module) ออกจากระบบคำนวณภาษีและประกันสังคมอย่างเด็ดขาด แต่เชื่อมต่อกันผ่าน Database Relations และ Service FunctionsEncapsulation: ควบคุมสิทธิ์การเข้าถึงผ่านระบบ Auth ของ Supabase โดยพนักงานทั่วไปจะมองเห็นและแก้ไขได้เฉพาะข้อมูลลาของตนเอง ส่วน HR จะมีสิทธิ์จัดการภาพรวมทั้งหมด   ละกรอกรหัส PIN (1234)
